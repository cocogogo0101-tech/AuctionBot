# 🔥 AuctionBot - السماء الجنوبية

بوت ديسكورد احترافي لإدارة المزادات مع قاعدة بيانات PostgreSQL/Supabase

## ✨ المميزات

### 🎯 المميزات الأساسية
- ✅ إنشاء مزادات تفاعلية بأزرار
- ✅ مزايدة سريعة (+) أو مبلغ مخصص
- ✅ نظام صلاحيات (رتبة رواد المزاد)
- ✅ تسجيل تفصيلي لكل مزاد
- ✅ دعم تنسيق الأرقام (k, m)
- ✅ حفظ كل شيء في قاعدة بيانات

### 🆕 التحديثات الجديدة
- 🛑 **نظام إلغاء المزاد**: إمكانية إلغاء المزادات النشطة من قبل الإدارة
- 📚 **سجل المزادات**: عرض المزادات السابقة مع تفاصيلها
- 📥 **تصدير البيانات**: تصدير بيانات المزادات كملف CSV
- 🔒 **حماية السيرفر**: تقييد البوت على سيرفر واحد فقط (Guild ID Lock)

---

## 📋 المتطلبات

- Python 3.10+
- PostgreSQL أو Supabase
- Discord Bot Token

---

## 🚀 التثبيت والإعداد

### 1️⃣ تثبيت المكتبات
```bash
pip install -r requirements.txt
```

### 2️⃣ إعداد ملف `.env`
انسخ ملف `.env.example` إلى `.env` وعدّل القيم:

```env
# Discord Bot Token
DISCORD_TOKEN=your_discord_bot_token_here

# Database Connection String
DATA=postgresql://user:password@host:port/database

# 🔒 Guild ID Lock (اختياري)
ALLOWED_GUILD_ID=1234567890
```

### 3️⃣ إعداد قاعدة البيانات
البوت سينشئ الجداول تلقائياً عند التشغيل الأول:
- `auctions` - جدول المزادات
- `bids` - جدول المزايدات
- `settings` - جدول الإعدادات

### 4️⃣ تشغيل البوت
```bash
python bot.py
```

---

## 🔧 الإعدادات

### تكوين السيرفر
استخدم الأوامر التالية لإعداد البوت:

```
/set_auction_role @رتبة_الرواد
/set_auction_channel #قناة_المزادات
/set_log_channel #قناة_اللوق
/set_emoji fire 🔥
```

### 🔒 نظام حماية السيرفر (Guild ID Lock)

لتفعيل الحماية، أضف `ALLOWED_GUILD_ID` في ملف `.env`:

```env
ALLOWED_GUILD_ID=1234567890
```

**كيف يعمل:**
- ✅ البوت سيعمل فقط في السيرفر المحدد
- 🚫 إذا تمت إضافته لسيرفر آخر، سيخرج تلقائياً
- 🧹 عند التشغيل، سيخرج من جميع السيرفرات غير المصرح بها
- 📨 يرسل رسالة توضيحية قبل المغادرة

**لتعطيل الحماية:**
- احذف السطر أو اتركه فارغاً في ملف `.env`

---

## 📖 الأوامر

### أوامر المزادات

#### `/لوحة_مزاد`
إنشاء لوحة مزايدة جديدة (إدارة فقط)

**المعاملات:**
- `start`: سعر البداية (مثال: 500k أو 1m)
- `min_inc`: أقل زيادة (مثال: 50k)
- `duration`: مدة المزاد بالدقائق
- `channel`: قناة المزاد (اختياري)

**مثال:**
```
/لوحة_مزاد start:1m min_inc:100k duration:30
```

#### `/إلغاء_مزاد` 🆕
إلغاء مزاد نشط (إدارة فقط)

**المعاملات:**
- `message_id`: ID رسالة المزاد

**مثال:**
```
/إلغاء_مزاد message_id:1234567890
```

**الحصول على ID الرسالة:**
1. انقر بزر الفأرة الأيمن على رسالة المزاد
2. اختر "نسخ معرف الرسالة"
3. (تأكد من تفعيل وضع المطور في إعدادات Discord)

#### `/سجل_المزادات` 🆕
عرض المزادات السابقة

**المعاملات:**
- `limit`: عدد المزادات (افتراضي: 10، الحد الأقصى: 50)

**مثال:**
```
/سجل_المزادات limit:20
```

#### `/تصدير_مزادات` 🆕
تصدير بيانات المزادات كملف CSV (إدارة فقط)

**المعاملات:**
- `limit`: عدد المزادات (افتراضي: 100، الحد الأقصى: 500)

**مثال:**
```
/تصدير_مزادات limit:200
```

**البيانات المصدرة:**
- Auction ID
- Started At
- Ended At
- Duration (minutes)
- Start Price
- Final Price
- Winner ID
- Winner Username
- Creator ID
- Total Bids
- Status (Completed/Cancelled/No Sale)

### أوامر الإعدادات

#### `/set_auction_role`
تحديد رتبة رواد المزاد (إدارة فقط)

#### `/set_auction_channel`
تحديد قناة المزادات الافتراضية (إدارة فقط)

#### `/set_log_channel`
تحديد قناة اللوق للمزادات (إدارة فقط)

#### `/set_emoji`
تعيين إيموجي مخصص (إدارة فقط)

**الأنواع المتاحة:**
- `fire` 🔥
- `crown` 🏆
- `time` ⏳
- `money` 💰

---

## 💾 قاعدة البيانات

### جدول auctions
```sql
id               SERIAL PRIMARY KEY
guild_id         BIGINT
channel_id       BIGINT
message_id       BIGINT
start_price      BIGINT
current_price    BIGINT
min_increase     BIGINT
created_by       BIGINT
started_at       TIMESTAMP WITH TIME ZONE
ended_at         TIMESTAMP WITH TIME ZONE
winner_id        BIGINT
ended            BOOLEAN
cancelled        BOOLEAN  -- 🆕
```

### جدول bids
```sql
id               SERIAL PRIMARY KEY
auction_id       INTEGER (FK -> auctions.id)
user_id          BIGINT
amount           BIGINT
created_at       TIMESTAMP WITH TIME ZONE
```

### جدول settings
```sql
key              TEXT PRIMARY KEY
value            TEXT
```

---

## 🎨 التخصيص

### ملف `config.json`
```json
{
  "auction_role_id": 0,
  "auction_channel_id": 0,
  "auction_log_channel": 0,
  "commission_percent": 20,
  "emojis": {
    "fire": "🔥",
    "crown": "🏆",
    "time": "⏳",
    "money": "💰"
  }
}
```

يمكنك تعديل:
- الإيموجيات المستخدمة
- نسبة العمولة (للاستخدام المستقبلي)
- القنوات والرتب الافتراضية

---

## 🔄 حالات المزاد

### ✅ مكتمل (Completed)
- المزاد انتهى بفائز
- تم حفظ الفائز والسعر النهائي

### 🚫 ملغي (Cancelled)
- تم إلغاؤه من قبل الإدارة
- يتم حفظ آخر سعر ومزايد
- لا يعتبر فوزاً للمزايد الأخير

### ❌ لم يتم البيع (No Sale)
- المزاد انتهى بدون أي مزايدات
- لا يوجد فائز

---

## 📊 تنسيق الأرقام

البوت يدعم تنسيق الأرقام بشكل ذكي:

**الإدخال:**
- `1000000` أو `1m` → 1 مليون
- `500000` أو `500k` → 500 ألف
- `1500` → 1,500

**العرض:**
- `1000000` → 1m
- `500000` → 500k
- `1500` → 1.5k

---

## 🛠️ الملفات

```
├── bot.py              # الملف الرئيسي
├── db.py               # طبقة قاعدة البيانات
├── web.py              # خادم Flask للـ keep-alive
├── config.json         # ملف الإعدادات
├── requirements.txt    # المكتبات المطلوبة
├── .env               # متغيرات البيئة (لا تشاركه!)
└── .env.example       # مثال لملف البيئة
```

---

## 🔐 الأمان

### نصائح الأمان:
1. ✅ **لا تشارك** ملف `.env` أبداً
2. ✅ استخدم **ALLOWED_GUILD_ID** لتقييد البوت
3. ✅ امنح صلاحيات الإدارة **فقط للموثوقين**
4. ✅ احفظ **نسخة احتياطية** من قاعدة البيانات
5. ✅ راقب **لوقات المزادات** بانتظام

---

## 🐛 حل المشاكل

### البوت لا يستجيب
- تأكد من تفعيل `Intents` في Discord Developer Portal
- تحقق من صحة الـ Token في ملف `.env`

### مشاكل قاعدة البيانات
- تأكد من صحة `DATA` connection string
- تحقق من وجود صلاحيات الكتابة في قاعدة البيانات

### البوت يخرج من السيرفر تلقائياً
- تأكد من `ALLOWED_GUILD_ID` في ملف `.env`
- تأكد من أن Guild ID صحيح

---

## 📝 التحديثات المستقبلية (اختياري)

- [ ] تمديد وقت المزاد تلقائياً
- [ ] نظام النقاط/الإحصائيات
- [ ] البلاك ليست
- [ ] معاينة قبل النشر
- [ ] تنبيهات ذكية
- [ ] نظام العمولة الفعلي
- [ ] السعر الاحتياطي

---

## 👨‍💻 المطور

**Dark | دارك**
- السماء الجنوبية
- نظام مزادات احترافي

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر للاستخدام الشخصي.

---

## 🙏 الشكر

شكراً لاستخدامك AuctionBot! 
إذا واجهت أي مشاكل أو لديك اقتراحات، لا تتردد في التواصل.

---

**صنع بـ ❤️ للسماء الجنوبية**
