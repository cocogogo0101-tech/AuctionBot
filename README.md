# 🔥 AuctionBot - السماء الجنوبية

بوت ديسكورد احترافي لإدارة المزادات مع قاعدة بيانات PostgreSQL

**المطور:** دارك  
**النسخة:** 3.0.0 Railway Edition

---

## ⚡ التثبيت السريع على Railway

### 1️⃣ إنشاء قاعدة بيانات

**خيار 1: Railway PostgreSQL (موصى به)**
```bash
1. New Project → Add PostgreSQL
2. انسخ DATABASE_URL من Variables
```

**خيار 2: Supabase (مجاني)**
```bash
1. سجل في supabase.com
2. Create New Project
3. Settings → Database → Connection String (Session mode)
```

### 2️⃣ إعداد Discord Bot

```bash
1. اذهب إلى https://discord.com/developers/applications
2. New Application → Bot
3. انسخ Token
4. Bot → Intents:
   ✅ SERVER MEMBERS INTENT
   ✅ MESSAGE CONTENT INTENT
   ✅ PRESENCE INTENT
5. OAuth2 → URL Generator:
   Scopes: bot, applications.commands
   Permissions: Administrator
6. انسخ الرابط وأضف البوت لسيرفرك
```

### 3️⃣ Deploy على Railway

```bash
1. New Project → Deploy from GitHub repo
2. Variables → Add Variables:
   
   DISCORD_TOKEN=your_token_here
   DATA=your_database_url_here
   ALLOWED_GUILD_ID=your_server_id (اختياري)

3. Deploy!
```

### 4️⃣ التحقق

```bash
# في Railway Logs، ابحث عن:
✅✅✅ نجحنا! البوت شغال 100% ✅✅✅
```

---

## 📖 الأوامر

### أوامر المزادات

#### `/مزاد`
إنشاء مزاد جديد

```
/مزاد start:1m min_inc:100k duration:30
```

**المعاملات:**
- `start` - سعر البداية (مثال: 500k أو 1m)
- `min_inc` - أقل زيادة (مثال: 50k)
- `duration` - مدة المزاد بالدقائق

#### `/إلغاء_مزاد`
إلغاء مزاد نشط (إدارة فقط)

```
/إلغاء_مزاد message_id:1234567890
```

**للحصول على ID الرسالة:**
1. انقر يمين على رسالة المزاد
2. نسخ معرف الرسالة

#### `/سجل_المزادات`
عرض المزادات السابقة

```
/سجل_المزادات limit:20
```

#### `/تصدير_مزادات`
تصدير البيانات كملف CSV (إدارة فقط)

```
/تصدير_مزادات limit:100
```

---

## 🎯 المميزات

### ✅ الأساسيات
- مزادات تفاعلية بأزرار
- مزايدة سريعة (+) أو مبلغ مخصص
- حفظ تلقائي في قاعدة البيانات
- دعم تنسيق الأرقام (k, m)

### 🆕 الجديد في v3.0
- ✅ معالجة تلقائية للمسافات
- ✅ إشارات واضحة للنجاح/الفشل
- ✅ كود نظيف 100%
- ✅ محسّن لـ Railway
- ✅ نظام Self-Healing متقدم

### 🛡️ الحماية
- إعادة اتصال تلقائي
- معالجة ذكية للأخطاء
- حماية من Rate Limiting
- Guild ID Lock (اختياري)

---

## 🔧 البنية

```
اسم المشروع/
├── bot.py              # الملف الرئيسي
├── db.py               # قاعدة البيانات
├── web.py              # Health check
├── requirements.txt    # المكتبات
├── Procfile           # Railway config
├── runtime.txt        # Python version
├── .env.example       # مثال للبيئة
├── .gitignore         # Git ignore
└── README.md          # هذا الملف
```

---

## 🐛 حل المشاكل

### ❌ فشلنا! Discord Token خاطئ
```bash
# الحل:
1. تحقق من DISCORD_TOKEN في Railway Variables
2. تأكد من عدم وجود مسافات
3. لا تستخدم علامات اقتباس
```

### ❌ Database connection failed
```bash
# الحل:
1. تحقق من DATA في Railway Variables
2. تأكد من صحة Connection String
3. في Supabase: استخدم Session mode
```

### ⚠️ الأوامر لا تظهر
```bash
# الحل:
1. انتظر 5-10 دقائق
2. تحقق من تفعيل Intents في Discord Portal
3. تأكد من صلاحيات البوت في السيرفر
```

---

## 📊 قاعدة البيانات

### جدول auctions
```sql
id, guild_id, channel_id, message_id,
start_price, current_price, min_increase,
created_by, started_at, ended_at,
winner_id, ended, cancelled
```

### جدول bids
```sql
id, auction_id, user_id, amount, created_at
```

---

## 🔐 الأمان

### ⚠️ مهم جداً:
1. **لا تشارك** ملف `.env`
2. **لا تشارك** DISCORD_TOKEN
3. استخدم **ALLOWED_GUILD_ID** لتقييد البوت
4. احفظ **نسخة احتياطية** من قاعدة البيانات

---

## 📞 الدعم

### إذا واجهت مشاكل:

1. **شوف Railway Logs:**
   ```bash
   Deployments → Latest → View Logs
   ```

2. **ابحث عن:**
   ```
   ✅✅✅ نجحنا! البوت شغال 100% ✅✅✅  # نجاح
   ❌❌❌ فشلنا! ...                    # فشل
   ```

3. **تحقق من:**
   - DISCORD_TOKEN صحيح؟
   - DATA صحيح؟
   - Intents مفعلة؟
   - البوت في السيرفر؟

---

## 🎨 التخصيص

### تغيير الإيموجيات
عدّل في `bot.py`:
```python
embed = discord.Embed(title="🔥 المزاد مشتعل 🔥", ...)
```

### تغيير الألوان
```python
embed = discord.Embed(..., color=0x9b59b6)  # بنفسجي
```

**ألوان مقترحة:**
- `0xe74c3c` - أحمر
- `0x3498db` - أزرق
- `0x2ecc71` - أخضر
- `0xf39c12` - برتقالي

---

## 📈 التحديثات القادمة

- [ ] تمديد تلقائي للمزاد
- [ ] نظام النقاط
- [ ] البلاك ليست
- [ ] حساب العمولة
- [ ] إحصائيات متقدمة

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر للاستخدام الشخصي.

**المطور:** دارك  
**السيرفر:** السماء الجنوبية  
**النسخة:** 3.0.0 Railway Edition

---

**🔥 بالتوفيق يا دارك! 🔥**
