# 📝 سجل التحديثات (Changelog)

## [v2.0.0] - 2024-02-01

### ✨ ميزات جديدة

#### 🛑 نظام إلغاء المزاد
- أمر `/إلغاء_مزاد` للإدارة لإلغاء المزادات النشطة
- تحديث تلقائي لرسالة المزاد عند الإلغاء
- إرسال تقرير مفصل لقناة اللوق عند الإلغاء
- حفظ حالة الإلغاء في قاعدة البيانات

#### 📚 سجل المزادات
- أمر `/سجل_المزادات` لعرض المزادات السابقة
- عرض حتى 50 مزاد سابق
- معلومات مفصلة: التاريخ، الحالة، الفائز، السعر النهائي
- تصنيف المزادات حسب الحالة (مكتمل/ملغي/لم يتم البيع)

#### 📥 تصدير البيانات
- أمر `/تصدير_مزادات` لتصدير البيانات كملف CSV
- تصدير حتى 500 مزاد
- بيانات شاملة: ID المزاد، التواريخ، المدة، الأسعار، الفائزين، عدد المزايدات
- مناسب للتحليل والأرشفة

#### 🔒 نظام حماية السيرفر (Guild ID Lock)
- متغير جديد في `.env`: `ALLOWED_GUILD_ID`
- تقييد البوت على سيرفر واحد فقط
- الخروج التلقائي من السيرفرات غير المصرح بها
- تنظيف السيرفرات عند التشغيل
- إرسال رسالة توضيحية قبل المغادرة

### 🔧 تحسينات

#### قاعدة البيانات
- إضافة عمود `cancelled` في جدول `auctions`
- إضافة indexes لتحسين الأداء:
  - `idx_auctions_guild_id`
  - `idx_bids_auction_id`
- دالة `cancel_auction()` لإلغاء المزادات
- دالة `get_auction_history()` لجلب السجل
- دالة `get_auction_stats()` للإحصائيات المفصلة
- دالة `get_user_stats()` لإحصائيات المستخدمين

#### واجهة المستخدم
- تحسين embeds للمزادات الملغية
- ألوان مميزة للمزادات الملغية (أحمر)
- رسائل خطأ أوضح وأكثر وصفية
- تحسين عرض الأرقام في ملف CSV

#### الكود
- إضافة تعليقات توضيحية بالعربية
- تحسين معالجة الأخطاء
- تنظيم أفضل للكود
- استيراد `csv` و `StringIO` للتصدير

### 📚 التوثيق
- README.md شامل ومفصل
- CHANGELOG.md لتوثيق التحديثات
- .env.example محدث مع شرح واضح
- توثيق كامل لجميع الأوامر الجديدة

### 🔍 تفاصيل تقنية

**ملفات محدثة:**
- `bot.py`: إضافة 3 أوامر جديدة + نظام الحماية
- `db.py`: إضافة 4 دوال جديدة + تحسين الجداول
- `requirements.txt`: تحديد إصدارات المكتبات
- `.env.example`: إضافة `ALLOWED_GUILD_ID`

**أوامر جديدة:**
1. `/إلغاء_مزاد` - إلغاء مزاد نشط
2. `/سجل_المزادات` - عرض السجل
3. `/تصدير_مزادات` - تصدير CSV

**Events جديدة:**
- `on_guild_join`: التحقق من الحماية عند الانضمام
- تحديث `on_ready`: التحقق من السيرفرات الحالية

---

## [v1.0.0] - النسخة الأصلية

### المميزات الأساسية
- إنشاء مزادات تفاعلية
- نظام المزايدة (سريع + مخصص)
- تسجيل المزايدات في قاعدة البيانات
- نظام الصلاحيات (رتبة رواد المزاد)
- تقارير مفصلة للمزادات
- دعم تنسيق الأرقام (k, m)
- قنوات مخصصة للمزادات واللوق

---

## 🔮 التحديثات القادمة

### قيد التخطيط
- [ ] تمديد وقت المزاد التلقائي
- [ ] نظام النقاط والإحصائيات للمستخدمين
- [ ] البلاك ليست
- [ ] معاينة المزاد قبل النشر
- [ ] تنبيهات ذكية للمزايدين
- [ ] حساب العمولة التلقائي
- [ ] نظام السعر الاحتياطي

### مقترحات مرحب بها
إذا كان لديك اقتراحات لتطوير البوت، نرحب بها!

---

**آخر تحديث: 2024-02-01**
**الإصدار الحالي: v2.0.0**
